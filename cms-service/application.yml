#==============================================================================================
# MySQL Database configuration
# Spring Boot 2.0 includes HikariDataSource by default
# spring.datasource.type = com.zaxxer.hikari.HikariDataSource
#==============================================================================================
spring:
   datasource:
      initialization-mode: ALWAYS
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/zscodewfdb?autoReconnect=true&useSSL=false&nullCatalogMeansCurrent=true
      username: zscodedbuser
      password: X4Kvti6FCgVZyVph
      hikari:
         connection-timeout: 20000
         minimum-idle: 5
         maximum-pool-size: 12
         idle-timeout: 300000
         max-lifetime: 1200000
         auto-commit: true        
spring:
   jpa:
      properties:
         hibernate:
            dialect: org.hibernate.dialect.MySQL57Dialect
            new_generator_mappings: false
            format_sql: true 
            search.lucene_version: LATEST
      hibernate:
         ddl-auto: update
#        ddl-auto: create
#        ddl-auto: create-drop